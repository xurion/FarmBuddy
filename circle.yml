dependencies:
  pre:
    - sudo apt-get install lua5.2
    - wget https://github.com/keplerproject/luarocks/tarball/master luarocks.tar.gz
    - tar -zxvf luarocks.tar.gz luarocks
    - ./luarocks/configure
test:
  override:
    - busted
